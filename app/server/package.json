{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "bun src/server.ts",
    "dev": "bun --watch src/server.ts",
    "build": "bun run clean && bun build ./src/server.ts --target node --outdir ./dist",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@types/jsonwebtoken": "^9.0.8",
    "body-parser": "^1.20.3",
    "cors": "2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/express": "^5.0.0",
    "rimraf": "^6.0.0"
  }
}
